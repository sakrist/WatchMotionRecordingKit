`createdUnix` remains the numeric timing source. `created` is a human-readable
UTC ISO 8601 representation of the same instant, generated by the shared
metadata contract. Metadata copy/finalization operations preserve the string.

During decoding, an older sidecar without `created` derives the string from its
existing `createdUnix`; writers always encode both values.
