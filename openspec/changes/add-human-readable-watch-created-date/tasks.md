## 1. Metadata contract

- [x] 1.1 Add and encode `created` as ISO 8601 UTC.
- [x] 1.2 Derive `created` when decoding an older sidecar.

## 2. Verification

- [x] 2.1 Add metadata encoding/decoding coverage and run the Xcode test target.
